# 07 - 数据库与存储

## 数据库选型

使用 SQLite，原因：
- 零配置，无需独立服务器
- 单文件存储，易于备份和迁移
- Python 标准库内置 `sqlite3`
- 适合单用户、中小规模数据量

## 表结构

### photo_scores 表

```sql
CREATE TABLE photo_scores (
    path              TEXT PRIMARY KEY,  -- 照片文件绝对路径
    caption           TEXT,              -- AI 生成的画面描述（80-200字）
    type              TEXT,              -- 照片类型（人物/风景/美食等，可多个用/分隔）
    memory_score      REAL,              -- 值得回忆度（0-100，一位小数）
    beauty_score      REAL,              -- 美观程度（0-100，一位小数）
    reason            TEXT,              -- 评分理由（不超过60字）
    width             INTEGER,           -- 照片宽度（像素）
    height            INTEGER,           -- 照片高度（像素）
    orientation       TEXT,              -- 方向：landscape/portrait/square
    used_at           TEXT,              -- 上次被选中展示的时间
    exif_json         TEXT,              -- 完整 EXIF 数据（JSON 格式）
    raw_json          TEXT,              -- VLM 原始返回（JSON 格式）
    exif_datetime     TEXT,              -- 拍摄时间
    exif_make         TEXT,              -- 相机制造商
    exif_model        TEXT,              -- 相机型号
    exif_iso          INTEGER,           -- ISO 感光度
    exif_exposure_time REAL,             -- 曝光时间
    exif_f_number     REAL,              -- 光圈值
    exif_focal_length REAL,              -- 焦距
    exif_gps_lat      REAL,              -- GPS 纬度
    exif_gps_lon      REAL,              -- GPS 经度
    exif_gps_alt      REAL,              -- GPS 海拔
    side_caption      TEXT,              -- 一句话文案（8-24字）
    exif_city         TEXT               -- 拍摄城市（中文）
);
```

注意：`server.py` 中使用了自增 `id` 字段（`INTEGER PRIMARY KEY AUTOINCREMENT`），但 `analyze_photos.py` 中以 `path` 为主键。实际运行中两种 schema 共存（通过 ALTER TABLE 兼容）。

## 数据写入

### 写入时机

- `analyze_photos.py` 每分析完一张照片立即 `INSERT OR REPLACE` + `COMMIT`
- 保证断点续跑：即使中途中断，已分析的数据不丢失

### 写入策略

```python
INSERT OR REPLACE INTO photo_scores (...) VALUES (...)
```

- 以 `path` 为主键，重复分析会覆盖旧数据
- `used_at` 字段通过子查询保留原值（不被覆盖）

## 数据读取

### 渲染模块读取

```sql
SELECT path, exif_json, side_caption, memory_score,
       exif_gps_lat, exif_gps_lon, exif_city
FROM photo_scores
WHERE exif_json IS NOT NULL
```

过滤条件：
- `exif_json IS NOT NULL`（必须有 EXIF 数据才能解析日期）
- 代码层面排除 screenshot

### Web 服务器读取

- 支持分页：`LIMIT {limit} OFFSET {offset}`
- 支持排序：`ORDER BY exif_datetime/memory_score/beauty_score`
- 支持筛选：`WHERE type LIKE '%{filter}%'`
- 支持搜索：`WHERE caption LIKE ? OR side_caption LIKE ? OR path LIKE ?`

## 文件存储

### 目录结构

```
data/
├── photos/              # 原始照片（IMAGE_DIR 指向）
├── output/              # 渲染输出
│   ├── photo_0.bin      # 第 0 张渲染照片
│   ├── photo_0.h        # 对应 C 头文件
│   ├── preview_0.png    # 抖动后预览
│   ├── ...
│   ├── latest.bin       # photo_0.bin 的副本
│   └── preview.png      # preview_0.png 的副本
├── photo_scores.db      # SQLite 数据库
└── world_cities_zh.csv  # 离线城市索引
```

### 文件命名规范

| 文件 | 命名规则 | 说明 |
|------|---------|------|
| 渲染照片 | `photo_{idx}.bin` | idx = 0 ~ DAILY_PHOTO_QUANTITY-1 |
| 预览图 | `preview_{idx}.png` | 与 bin 一一对应 |
| 头文件 | `photo_{idx}.h` | 数组名 `daily_bin_{idx}` |
| 兼容文件 | `latest.bin/h`, `preview.png` | 指向第 0 张 |

## Docker 数据持久化

推荐使用绑定挂载：

```yaml
volumes:
  - ./data:/app/data        # 数据库 + 城市索引
  - ./output:/app/output    # 渲染输出
  - /path/to/photos:/photos:ro  # 照片目录（只读）
```

## 备份策略

- 核心数据：`photo_scores.db`（包含所有分析结果）
- 备份频率：建议每周备份一次数据库
- 恢复方式：直接替换 .db 文件即可
- 验证命令：`sqlite3 photo_scores.db "PRAGMA integrity_check;"`
