.env
.git/
.github/
.kiro/
.DS_Store
__pycache__/
*.py[cod]
venv/
.venv/
node_modules/
old/
tmp/
logs/
filelist.txt
# 同时覆盖 Docker 与 Podman 的根目录及任意子目录 SQLite 文件。
*.db
*.db-*
**/*.db
**/*.db-*
# 整个 data/ 都不进镜像。它是运行期数据卷，容器启动时会被宿主目录挂载覆盖，
# 放进镜像的任何东西都会被遮蔽——城市索引以前就吃了这个亏，明明打进了镜像却读不到。
# 整体排除也比逐项列举安全：少一条规则就少一处漏排密钥或数据库的机会。
# 只读资源一律放 resources/，那里不受挂载影响。
data/
esp32/
# 文档不参与运行，却会被 COPY . . 带进镜像，使每次改文档都产生新的镜像内容，
# 让"镜像摘要对应某个提交"的交付记录不断失效。运行镜像只需要代码与必需资源。
docs/
