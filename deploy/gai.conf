# 保留 GNU C Library 默认优先级，仅提高 IPv4-mapped 地址优先级。
# 双栈域名优先 IPv4，只有 AAAA 记录的目标仍可使用 IPv6。
precedence ::1/128       50
precedence ::/0          40
precedence 2002::/16     30
precedence ::/96         20
precedence ::ffff:0:0/96 100
